To run this program you need to run the .jar file with the command:

    java -jar untitled1-1.0-SNAPSHOT.jar <path_to_file>

you can run the tests in the root directory using the command:

    mvn test
